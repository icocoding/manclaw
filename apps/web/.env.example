# 可选：显式指定前端直连的 API 地址。
# 默认留空，开发时会通过 Vite proxy 转发 /api 和 /health 到 127.0.0.1:18300。
# VITE_API_BASE_URL=http://127.0.0.1:18300

# 可选：修改 Vite 开发代理的后端目标地址。
# VITE_API_PROXY_TARGET=http://127.0.0.1:18300
